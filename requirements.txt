# Web / API stack (needed to boot)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4   # >=2.11.7 required by realtime (supabase 2.31.0) dependency chain
pydantic-settings==2.7.1
python-dotenv==1.0.1
supabase==2.31.0   # >=2.31 accepts new-format sb_publishable_/sb_secret_ API keys
slowapi==0.1.9
numpy==2.2.1

# Model / scoring via NDIF (needed from Phase 1 onward)
# Verified together: nnsight 0.7.0 / torch 2.12.0 (CPU) / transformers 5.10.2.
# transformers 5 is required for the `olmo3` architecture (OLMo-3-32B); it also
# changed decoder layer .output to the hidden tensor (handled in ndif_client).
nnsight==0.7.0
# torch CPU build — in the Dockerfile install with:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
transformers==5.10.2

# Dev / test
pytest==8.3.4
httpx==0.28.1
